{
  "name": "directory-jsonizer",
  "version": "0.1.1",
  "description": "A small library to convert all file contents in a directory and its all subdirectories into a single JSON string",
  "main": "src/index.js",
  "scripts": {
    "test": "NODE_PATH=./ mocha test/*.js",
    "lint": "eslint --fix src/**/*.js"
  },
  "keywords": [
    "directory",
    "json",
    "base64"
  ],
  "author": "AnnPin",
  "repository": {
    "type": "git",
    "url": "https://github.com/AnnPin/directory-jsonizer.git"
  },
  "license": "MIT",
  "dependencies": {
    "is-utf8": "^0.2.1"
  },
  "devDependencies": {
    "del": "^3.0.0",
    "eslint": "^4.11.0",
    "mocha": "^4.0.1",
    "power-assert": "^1.4.4"
  }
}
